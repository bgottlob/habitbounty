<h4>Welcome to HabitBounty</h4>
{{#if error}}<h6>{{error}}</h6>{{/if}}
<h5>Your balance is: $<span id="balance">{{balance}}</span></h5>
{{#if habits}}
<h5>Your habits:</h5>
<table>
  <tbody>
    {{#each habits}}
    <tr>
      <td>
        <input class="completeHabit" type="checkbox" value="{{id}}"
         data-reward="{{reward}}" {{isComplete this}}/>
      </td>
      <td>{{name}}</td>
      <td>${{reward}}</td>
    </tr>
    {{/each}}
  </tbody>
</table>
{{/if}}
